%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @relationships.each do |relationship|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(relationship.followed_user.email.downcase)}?s=40")
            = link_to relationship.followed_user.name, user_path(relationship.followed_user)
          %td.extra-padding= relationship.followed_user.queue_items.size
          %td.extra-padding= relationship.followed_user.followers.size
          %td.extra-padding
            = link_to relationship, method: :delete do
              %i.glyphicon.glyphicon-remove